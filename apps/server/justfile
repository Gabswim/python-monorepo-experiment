# postmodern-server

dev:
    uv run uvicorn postmodern.server.run:app --reload --port 8000

test:
    uv run pytest .

check:
    uv run ty check .

fmt:
    uv run ruff format .

lint:
    uv run ruff check --fix .

ci-fmt:
    uv run ruff format --check .

ci-lint:
    uv run ruff check .
